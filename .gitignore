node_modules/
dist/

# Auslieferungspakete (scripts/release-offline.sh). Enthalten das gespeicherte
# Docker-Image und sind rund 170 MB gross — sie gehoeren auf den USB-Stick,
# nicht ins Repo.
release/
.env
.env.bak
logs/
docs/.vitepress/cache/
docs/.vitepress/dist/
docker/.env

# Lokale Daten (Secrets). Enthaelt nur noch users.json — den Legacy-Konten-
# Fallback, den importLegacyJsonUsers() beim Boot einliest. Faellt mit AP7
# (Anmeldung haerten) weg.
/data/

# Lokales Claude-Tooling (Workspace-Konvention: nie committen)
.claude/

# Testdaten mit erfundenen Projekten/Personen (nicht ins Repo)
test-uploads/

# graphify-Code-Graph (generiertes Analyse-Artefakt, ~9 MB, lokal regenerierbar)
graphify-out/
