# ============================================================
# PATIO Docker — Umgebungsvariablen
# Kopiere nach docker/.env und fuelle aus:
#   cp docker/.env.example docker/.env
# ============================================================

# ── PostgreSQL ────────────────────────────────────────────────
POSTGRES_DB=patio
POSTGRES_USER=patio
POSTGRES_PASSWORD=                # Generiere: openssl rand -hex 24
POSTGRES_PORT=5432
